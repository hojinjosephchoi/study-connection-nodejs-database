doctype html
html
	head
		meta(charset='utf-8')
		title Delete
	body
		h1 
			a(href='/topic') Server Side Javascript
		ul
			each topic in topics
				li
					a(href='/topic/' + topic.id)= topic.title
		article
			form(action='/topic/' + topic.id + '/delete' method='post')
				h1= 'Are you sure to delete? : ' + topic.title
				p
					input(type='submit')
				a(href='/topic/' + topic.id) No